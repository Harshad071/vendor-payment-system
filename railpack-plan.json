{
  "version": "0.1",
  "copy": [
    "backend/**"
  ],
  "build": {
    "install": "cd backend && pnpm install",
    "build": "cd backend && pnpm run build && cp src/database/ormconfig.ts dist/database/ormconfig.ts"
  },
  "deploy": {
    "start": "cd backend && pnpm run start"
  }
}
